'use client';
import { useCallback,useEffect,useState } from 'react';
import { PageHeader } from '@/components/affiliates/shared/ui';
export default function Integrations(){const [items,setItems]=useState<{name:string;status:string;detail:string}[]>([]);const [error,setError]=useState('');const [busy,setBusy]=useState(false);const load=useCallback(async()=>{setBusy(true);setError('');try{const r=await fetch('/api/affiliates/admin/integrations');const d=await r.json();if(!r.ok)throw new Error(d.error);setItems(d.connections);}catch(e){setError(e instanceof Error?e.message:'Could not check connections.');}finally{setBusy(false);}},[]);useEffect(()=>{void load();},[load]);return <div><PageHeader eyebrow="IQON Supplements" title="Business connections" description="Check the services that power this affiliate program." actions={<button className="button button-outline" onClick={()=>void load()} disabled={busy}>{busy?'Checking…':'Refresh checks'}</button>}/>{error&&<p role="alert">{error}</p>}<div className="grid md:grid-cols-2 gap-5">{items.map(item=><article className="glass-surface rounded-lg p-6" key={item.name}><p className="text-xs uppercase tracking-wider text-slate-500 mb-4">{item.status}</p><h2 className="text-2xl mb-3">{item.name}</h2><p className="text-sm text-slate-500">{item.detail}</p></article>)}</div></div>;}
