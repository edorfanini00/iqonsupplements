"use client";
import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { CartLine, useStore } from "../store-shell";
import { lineKey, money } from "@/lib/catalog";
export default function CheckoutPage(){const {cart,subtotal,count}=useStore();return <main id="main"><div className="checkout-heading section-pad"><h1>Your selection.</h1><p>Checkout preview · Sample products and prices. No order will be placed.</p></div>{cart.length?<div className="checkout-layout section-pad"><div className="checkout-lines">{cart.map(item=><CartLine key={lineKey(item)} item={item}/>)}</div><aside className="checkout-summary"><h2>Order summary</h2><dl><div><dt>Products ({count})</dt><dd>{money(subtotal)}</dd></div><div><dt>Shipping</dt><dd>Confirmed at launch</dd></div><div><dt>Tax</dt><dd>Confirmed at launch</dd></div><div><dt>Sample subtotal</dt><dd>{money(subtotal)}</dd></div></dl><p>Payment is not enabled for this store preview. Your bag stays in this browser so you can continue exploring the collection.</p><Link href="/collections/all" className="button button-dark full-width">Continue shopping <ArrowRight size={17}/></Link></aside></div>:<div className="section-pad checkout-empty"><h2>Your bag is waiting for its first essential.</h2><Link href="/collections/all" className="button button-dark">Explore the collection <ArrowRight size={17}/></Link></div>}</main>;}
