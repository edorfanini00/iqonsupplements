import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, ArrowUpRight } from "lucide-react";
import { journalArticles } from "@/lib/journal";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const article=journalArticles.find(a=>a.slug===slug);return {title:article?`${article.title} — Latest News`:"Latest News",description:article?.dek};}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const article=journalArticles.find(a=>a.slug===slug);if(!article)notFound();return <main id="main" className="journal-article"><header className="article-heading section-pad"><Link className="article-back" href="/journal"><ArrowLeft size={16}/>Latest News</Link><p className="eyebrow">{article.category} · {article.readTime}</p><h1>{article.title}</h1><p>{article.dek}</p></header><img className="article-cover" src={article.image} alt={article.imageAlt} width={1800} height={1200}/><div className="article-body"><p className="article-byline">IQON EDITORIAL</p>{article.sections.map(section=><section key={section.heading}><h2>{section.heading}</h2>{section.paragraphs.map(p=><p key={p}>{p}</p>)}</section>)}{article.sources&&<aside className="article-sources" aria-label="Article sources"><h2>Further reading</h2>{article.sources.map(source=><a key={source.url} href={source.url} target="_blank" rel="noopener noreferrer">{source.label} ↗</a>)}</aside>}<div className="article-next"><p className="eyebrow">EXPLORE AT YOUR OWN PACE</p><Link className="under-link" href={article.category==="SKINCARE NOTES"?"/collections/skincare":"/collections/supplements"}>Meet the collection<ArrowUpRight size={17}/></Link></div></div></main>;}
